# English source reviews

This directory stores English review Markdown for AI-assisted translation.

It is intentionally outside `docs/`, so Docusaurus will not publish it. Put the matching public Chinese translation in `docs/games/<slug>.mdx`.
